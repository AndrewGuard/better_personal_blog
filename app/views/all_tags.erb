<div class="container">
  <h3> A list of all tags </h3>
</div>

<% @tags.order('created_at DESC').each do |tag| %>
  <div class="container">
    <p><a href="/tags/<%= tag.id%>"> <%= tag.tag_title%> </a></p>
    <p>Created <%= tag.created_at %> </p>
  </div>
<% end %>
